---
name: no-tools-agent
description: Agent frontmatter is missing the tools field — triggers agent/missing-tools
---

This agent prompt has valid frontmatter but omits the required `tools` key.
The `tools` field declares which Claude tools this agent is allowed to use.
