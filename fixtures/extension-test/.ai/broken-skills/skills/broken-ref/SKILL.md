---
name: broken-ref
description: References a script that does not exist — triggers plugin/broken-paths
---

Run this helper to deploy:

```bash
${CLAUDE_SKILL_DIR}/scripts/deploy.sh
${CLAUDE_SKILL_DIR}/bin/setup
```

Neither `scripts/deploy.sh` nor `bin/setup` exist relative to this skill directory.
