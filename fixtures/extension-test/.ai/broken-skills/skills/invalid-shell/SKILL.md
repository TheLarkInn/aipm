---
name: invalid-shell
description: Shell field is set to zsh which is not allowed — triggers skill/invalid-shell
shell: zsh
---

Only `bash` and `powershell` are valid shell values. `zsh`, `fish`, `sh`, etc. are rejected.
