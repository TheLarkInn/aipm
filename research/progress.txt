## 2026-05-04 — Feature #1: Scaffold libaipm-engine-spec crate

Scaffolded the new crate at crates/libaipm-engine-spec/ with stub modules
(lib.rs, types.rs, helpers.rs, generated.rs), an empty build.rs, and a
stub src/bin/export-schema.rs. Workspace Cargo.toml gained the deps
phf 0.13, phf_codegen 0.13, schemars 1, jsonschema 0.46, prettyplease
0.2, bitflags 2 (and the libaipm-engine-spec path entry). The new crate's
build-dependencies pin syn 2 / proc-macro2 1 / quote 1.

Workspace pickup is automatic via members = ["crates/*"]. Verified with
cargo build --workspace and the cargo-verifier subagent:
  build PASS / test PASS / clippy PASS / fmt PASS / coverage 89.97%.

Next: feature #2 — populate types.rs with the canonical schemars-derived
types per spec section 5.2.

## 2026-05-04 — Feature #2: Canonical Rust types in types.rs

Implemented all 13 canonical types per spec §5.2:
EngineApiSchemaFile, EngineBootstrap, EngineApi, ManifestFieldSpec,
Constraints, ConventionFile, SizeLimits, ToolCall, HookEvent, FeatureSpec,
FeatureKind, ToolCompatibility, ToolSupport, EngineSuggestions. All carry
#[derive(Debug, Clone, PartialEq, Eq, Deserialize, Serialize, JsonSchema)]
and #[serde(deny_unknown_fields)] (omitted only on SizeLimits where
#[serde(flatten)] collects unknown numeric keys into the BTreeMap). Used
BTreeMap throughout for deterministic key order. META_SCHEMA_VERSION = "1.0.0"
re-exported at crate root from lib.rs.

Side-fix: schemars added to [dependencies] (it was scaffolded only into
[build-dependencies] in feature #1, but the runtime derive macro needs it
at compile time of the lib).

Verified via cargo-verifier: build/test/clippy/fmt clean, coverage 89.97%.

Next: feature #3 — bin/export-schema.rs implementation.

## 2026-05-04 — Feature #3: bin/export-schema.rs

Implemented the schema-export binary at
crates/libaipm-engine-spec/src/bin/export-schema.rs. Uses
schemars::schema_for! on EngineApiSchemaFile, serialises pretty JSON, and
writes to schemas/engine-api.schema.json relative to CARGO_MANIFEST_DIR
(robust to invocation cwd) with create_dir_all on the parent.

EngineApiSchemaFile re-exported at the crate root so the bin can refer to
libaipm_engine_spec::EngineApiSchemaFile directly per spec §5.5.

Ran the binary and verified the generated 11.8KB JSON Schema 2020-12
document at schemas/engine-api.schema.json contains all 13 expected
$defs (EngineBootstrap, EngineApi, ManifestFieldSpec, Constraints,
ConventionFile, SizeLimits, ToolCall, HookEvent, FeatureSpec, FeatureKind,
ToolCompatibility, ToolSupport, EngineSuggestions). Committed alongside
the bin source.

Verified via cargo-verifier: build/test/clippy/fmt clean, coverage 89.97%.

Next: feature #4 — migrate research/engine-api-schema.json into the new
crate's data/ directory with the restructured fields ($schema URI,
meta_schema_version, structured manifest_fields, renamed *_notes,
hook_events arrays, features arrays).

## 2026-05-04 — Feature #4: Migrate + restructure data file

git mv'd both data files from research/ to
crates/libaipm-engine-spec/data/ (the schema JSON + the changelog).
Added top-level $schema URI and meta_schema_version "1.0.0" to the JSON.

Restructured copilot-cli's manifest_fields from 21 prose strings into
21 typed records with constraints (max_length, regex, default). Claude's
manifest_fields stays as []. Renamed detection_heuristics → detection_notes,
discovery_algorithm → discovery_notes, rules → rule_notes per Q3a.

Added hook_events arrays per engine: 27 entries for claude (PascalCase)
and 10 canonical camelCase entries for copilot-cli with PascalCase legacy
aliases attached. Added features arrays: 5 for claude (skill, agent, mcp,
hook, output_style), 6 for copilot-cli (skill, agent, mcp, hook, lsp,
extension).

Verified via Python: 8 top-level keys correct, 15/18 keys per engine api,
27/10 hook events, 5/6 features, 21 copilot manifest fields. Workflow
files in .github/workflows/reverse-binary-analysis.md still reference the
old paths — that's feature #33's scope.

Verified via cargo-verifier: build/test/clippy/fmt clean, coverage 89.97%.

Next: feature #5 — re-run export-schema and verify the committed schema
contains all canonical $defs (effectively a smoke test now that the bin
already wrote the file in feature #3).

## 2026-05-04 — Feature #5: Initial committed schema verification

Re-ran cargo run -p libaipm-engine-spec --bin export-schema. Result:
zero diff against the file committed in feature #3 (idempotent — same
inputs produce same output, as expected for a pure schemars derive).

Verified the committed schemas/engine-api.schema.json:
  $schema: https://json-schema.org/draft/2020-12/schema
  title: EngineApiSchemaFile
  $defs: 13/13 present (EngineBootstrap, EngineApi, ManifestFieldSpec,
    Constraints, ConventionFile, SizeLimits, ToolCall, HookEvent,
    FeatureSpec, FeatureKind, ToolCompatibility, ToolSupport,
    EngineSuggestions)

This feature was effectively a smoke test since the schema file was
already committed when bin/export-schema.rs landed in feature #3.

Verified via cargo-verifier: build/test/clippy/fmt clean, coverage 89.97%.

Next: feature #6 — implement build.rs validation step (parse data file,
validate against meta-schema via jsonschema crate, deserialize into
EngineApiSchemaFile, check meta_schema_version match).

## 2026-05-04 — Feature #6: build.rs validation step

Implemented build-time validation in
crates/libaipm-engine-spec/build.rs. Steps performed on every build:
  1. cargo:rerun-if-changed for data/engine-api-schema.json, the
     meta-schema, src/types.rs, build.rs
  2. read meta-schema + data file from disk
  3. parse both as serde_json::Value and build a jsonschema validator
     for the meta-schema (auto-detects draft 2020-12 from $schema)
  4. validator.validate(&data) — error path includes a remediation hint
     pointing to bin/export-schema
  5. deserialize the validated data into types::EngineApiSchemaFile
  6. assert parsed.meta_schema_version == types::META_SCHEMA_VERSION

Types are shared with the build script via #[path = "src/types.rs"]
mod types; that compiles against the same serde/schemars in
[build-dependencies]. cargo:rerun-if-changed is emitted via writeln!
to a locked stdout to sidestep the workspace print_stdout = "deny"
clippy lint.

The validation passes on the current data file: cargo build, test,
clippy, fmt all clean; coverage 89.97%.

Next: feature #7 — build.rs codegen for the generated Engine enum and
EngineSet bitflags into OUT_DIR/engine_data.rs.

## 2026-05-04 — Feature #7: codegen Engine enum + EngineSet bitflags

Extended build.rs to generate OUT_DIR/engine_data.rs via proc-macro2 +
quote, formatted with prettyplease::unparse(syn::parse2(tokens)?).

Generated artifacts:
  - pub enum Engine { Claude, CopilotCli } with #[derive(Debug, Clone,
    Copy, PartialEq, Eq, Hash)]
  - impl Engine { pub const ALL: &'static [Self]; pub const fn
    name(self) -> &'static str; pub fn from_name(&str) -> Option<Self> }
  - bitflags::bitflags! { pub struct EngineSet: u32 { const CLAUDE = 1u32;
    const COPILOT_CLI = 2u32; const ALL = Self::CLAUDE.bits() |
    Self::COPILOT_CLI.bits(); } } with the same derives

Variants are derived from parsed.engines via to_pascal_case (kebab → PascalCase)
and to_screaming_snake (kebab → SCREAMING_SNAKE). Bit values 1 << i.

Wired src/generated.rs as include!(concat!(env!("OUT_DIR"),
"/engine_data.rs")) and re-exported Engine + EngineSet from the crate
root in lib.rs.

Lint fixes during implementation:
  - clippy::option_if_let_else: replaced match chars.next() with
    chars.next().map_or_else(String::new, ...)
  - clippy::use_self: emit Self:: instead of Engine:: inside impl Engine
  - clippy::doc_markdown: backtick all type names in generated.rs doc

Added 4 inline smoke tests (engine_all_lists_known_variants,
engine_name_round_trips, engine_set_all_is_union_of_individual_bits,
meta_schema_version_is_semver_like). All pass.

Verified via cargo-verifier: build/test/clippy/fmt clean, coverage 89.99%.

Next: feature #8 — codegen the ENGINES const table with EngineSpec
records (name, package, version, marker_paths, marketplace_manifest_path,
manifest_search_paths, settings_paths, folder_conventions,
convention_files).

## 2026-05-04 — Feature #8: codegen ENGINES const table

Added pub struct EngineSpec to types.rs with the 9 &'static fields the
spec calls for (name, package, version, marker_paths,
marketplace_manifest_path, manifest_search_paths, settings_paths,
folder_conventions, convention_files: &[(filename, &[paths])]).

Extended build.rs with generate_engines_const() / generate_engine_entry()
to emit `pub const ENGINES: &[(Engine, EngineSpec)] = &[ ... ];` from
parsed.engines + parsed.apis. Schema-driven fields come straight from the
data; marker_paths and marketplace_manifest_path remain hand-rolled
engine-name-keyed helpers because the schema doesn't track plugin.json
locations directly. The copilot-cli marketplace path is now
".github/plugin/marketplace.json" (corrected from the prior `.toml` per
Q2b "schema wins").

Build-script bookkeeping:
  - silence dead_code on EngineSpec inside the build script's mod types
    via const _: Option<types::EngineSpec> = None; — avoids relying on
    #[allow] (which would trip allow_attributes = "warn")
  - lint fix: clippy::map_unwrap_or → map_or for the version lookup

Two new smoke tests:
  - engines_const_has_at_least_two_entries (>= 2 entries, claude +
    copilot-cli present)
  - engines_const_marketplace_paths_match_schema_wins_decision
    (claude .toml; copilot .json)

ENGINES is re-exported at the crate root. Total tests: 6 passing.

Verified via cargo-verifier: build/test/clippy/fmt clean, coverage 89.99%.

Next: feature #9 — codegen VALID_TOOLS as a phf::Set via phf_codegen
populated from every engine's tool_calls names + aliases.

## 2026-05-04 — Feature #9: codegen VALID_TOOLS phf::Set

Added write_valid_tools_phf() to build.rs. It flattens
parsed.apis.<engine>.tool_calls (names + aliases), sort_unstable +
dedup for reproducible output, and writes a phf_codegen-built
`pub static VALID_TOOLS: phf::Set<&'static str> = ...;` into
OUT_DIR/valid_tools.rs.

src/generated.rs gained a sub-module valid_tools_inner that include!s
the phf file. The wrapper carries
  #[expect(clippy::unreadable_literal, reason = "phf::Map's u64 hash
   key is generated by phf_codegen and won't pass the
   clippy::unreadable_literal threshold")]
which is needed because the generated phf hash key is a 20-digit u64
literal with no separators. The workspace's `allow_attributes = "warn"`
lint targets `#[allow]` specifically; `#[expect]` (the recommended
replacement) is not flagged.

VALID_TOOLS re-exported at the crate root. Two new smoke tests confirm
the set contains "bash", "Bash", "Edit", "Read", "Write",
"browser_navigate" and rejects unknowns + the empty string. Total tests:
8 passing.

Verified via cargo-verifier: build/test/clippy/fmt clean, coverage 90.00%.

Next: feature #10 — codegen TOOL_COMPATIBILITY const table mapping each
tool name to the EngineSet that supports it (shared → ALL,
engine_exclusive → that engine's bit).








