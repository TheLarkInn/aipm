## 2026-05-01 — Feature 1: Foundation types for unified discovery module — DONE

Implemented per spec `specs/2026-05-01-unified-discovery-and-copilot-skill-detection.md`.

Changes:
- Renamed `crates/libaipm/src/discovery.rs` → `crates/libaipm/src/discovery_legacy.rs`.
- Declared `mod discovery_legacy;` (private) in `lib.rs`.
- Added new module folder `crates/libaipm/src/discovery/` containing:
  - `mod.rs` — public API surface that re-exports both new types (Engine, Layout, DiscoveredSet, ScanCounts, SkipReason, FeatureKind) and legacy items (Error, DiscoveredFeature, SourceContext, DiscoveredSource, discover_features, discover_source_dirs, discover_claude_dirs).
  - `types.rs` — Engine, Layout, FeatureKind (moved from legacy), and the new DiscoveredFeature struct with shape { kind, engine, layout, source_root, feature_dir, path }.
  - `scan_report.rs` — DiscoveredSet, ScanCounts, SkipReason, with `counts()` aggregator and `is_empty()` helper.
- Updated `discovery_legacy.rs` to import FeatureKind from `crate::discovery::types::FeatureKind` (single source of truth).

Deviation from spec: the spec said `feature.rs` for the new types module. `clippy::module_name_repetitions` rejects `pub enum FeatureKind` and `pub struct DiscoveredFeature` inside a module named `feature`. Renamed to `types.rs` to follow the existing `manifest/types.rs` convention. Updated feature-list.json accordingly.

All callers continue to compile unchanged thanks to discovery/mod.rs re-exports. The new shape of DiscoveredFeature is accessible via `crate::discovery::types::DiscoveredFeature`; the legacy shape remains at `crate::discovery::DiscoveredFeature` until later spec features.

Tests added: 9 in types.rs + 10 in scan_report.rs covering Default, Clone, PartialEq, variant distinctness, counts aggregation across each FeatureKind, is_empty true/false, SkipReason variants.

Cargo verifier report:
- build: PASS
- test: PASS (2007 lib + 74 BDD scenarios + integration)
- clippy: PASS (zero warnings under -D warnings)
- fmt: PASS
- coverage: PASS (89.65% TOTAL branch, threshold 89%)
- overall: PASS

Forbidden patterns check (.unwrap, .expect, panic!, todo!, println!, eprintln!, dbg!): none in new files.

## 2026-05-01 — Feature 2: Engine-root inference helper — DONE

Added `crates/libaipm/src/discovery/source.rs` with `pub fn infer_engine_root(path: &Path, project_root: &Path) -> Option<(Engine, PathBuf)>`.

Behavior:
- Walks path.ancestors() upward until project_root is reached.
- Returns the FIRST ancestor whose file_name matches `.claude` (Claude), `.github` (Copilot), or `.ai` (Ai). Innermost wins, mirroring the unified-discovery design where each feature is associated with the engine that authored it, not the marketplace host that contains it.
- Handles non-UTF-8 path components via `to_string_lossy()` — invalid UTF-8 simply doesn't match any engine root.
- Returns None if path == project_root, or if no engine ancestor exists.

Re-exported from `discovery/mod.rs` as `pub use source::infer_engine_root`.

Tests added: 14 covering .claude canonical, .github canonical, .github/copilot/skills/<x> (issue #725 layout), .ai/<plugin>, nested .ai/<plugin>/.claude (innermost wins), no-engine-ancestor, path == project_root, project_root is engine dir, relative paths with and without dot prefix, deeply nested .ai > .github > .claude (innermost wins), unrelated dotted dirs ignored, path outside project_root, project_root at filesystem root.

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 89.69% (>= 89%)
- overall: PASS

## 2026-05-01 — Feature 3: Skill layout grammar (THE customer fix for #725) — DONE

Added `crates/libaipm/src/discovery/layout.rs` with `pub fn match_skill(path, engine, source_root) -> Option<DiscoveredFeature>` plus a private `pick_layout_for_skill` helper.

Behavior:
- Case A: any ancestor name between path's parent and source_root (exclusive) literally equals "skills" → match as Skill. This is the customer fix for #725 (`.github/copilot/skills/<x>/SKILL.md` works because "skills" is in the ancestors list).
- Case B: engine == Copilot AND grandparent (ancestors[1]) == "copilot" → match as Layout::CopilotSubroot (existing aipm accommodation `.github/copilot/<name>/SKILL.md`).
- pick_layout_for_skill pattern-matches the ancestors slice to choose the right Layout variant. Arms ordered most-specific-first:
  1. `[_, "skills", "copilot", ..]` → CopilotSubrootWithSkills (#725)
  2. `[_, "copilot", ..]` → CopilotSubroot
  3. `[_, "skills", "<.dot>", _, ..]` → AiNested
  4. `[_, "skills", _, ..]` if engine==Ai → AiPlugin
  5. default → Canonical

Re-exported as `pub use layout::match_skill` from `discovery/mod.rs`.

Tests (17 total): each of 5 layouts (canonical Claude, canonical Copilot, CopilotSubroot, CopilotSubrootWithSkills with #725's exact path, AiPlugin, AiNested), the all-three-skills #725 batch, flat skills layout, structural fields (feature_dir + path), plus 5 negative cases (skill at engine root, skill directly under copilot dir without a <name> sub-dir, copilot subroot only matches Copilot engine, skill with no skills ancestor, AiPlugin gated to Ai engine).

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 89.74% (>= 89%)
- overall: PASS

## 2026-05-01 — Feature 4: Layout matchers for non-skill features — DONE

Added five non-skill matchers to `crates/libaipm/src/discovery/layout.rs`:

- `match_agent(path, engine, source_root)` — `*.md` whose parent directory is named `agents`. Mirrors today's `classify_feature_kind` agent branch (`discovery_legacy.rs:265-266`). Instruction-file precedence is the dispatcher's responsibility (later spec feature).
- `match_hook(path, engine, source_root)` — `hooks.json` whose parent is either the engine source root (CopilotHookDetector accommodation) OR a directory literally named `hooks` (today's classify_feature_kind line 267-268).
- `match_plugin(path, engine, source_root)` — `aipm.toml` with grandparent `.ai`. Mirrors today's line 269-270.
- `match_marketplace(path, engine, source_root)` — `marketplace.json` with parent `.claude-plugin` and grandparent `.ai`. Mirrors lines 271-275.
- `match_plugin_json(path, engine, source_root)` — `plugin.json` with parent `.claude-plugin` and great-grandparent `.ai`. Mirrors lines 276-287.

All non-skill matchers return `Layout::Canonical` since skill is the only feature kind whose layout shape varies. `feature_dir` is set to `path.parent()` per spec.

Helpers added: `parent_name_lossy(path)` and `ancestor_name_lossy(path, depth)` for clean lookups.

Re-exported from `discovery/mod.rs` as `match_agent`, `match_hook`, `match_marketplace`, `match_plugin`, `match_plugin_json` alongside the existing `match_skill`.

Tests added: 20 covering canonical positive cases for each matcher, plus negative cases (wrong parent, wrong filename, wrong grandparent, missing engine root). The test file now has 37 total layout tests.

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 89.81% (>= 89%)
- overall: PASS

## 2026-05-01 — Feature 5: Instruction classifier — DONE

Added `crates/libaipm/src/discovery/instruction.rs` with `pub fn classify(file_name, path, engine, source_root) -> Option<DiscoveredFeature>` that recognizes three filename shapes case-insensitively:

1. **Exact name** in `INSTRUCTION_FILENAMES` (`claude.md`, `agents.md`, `copilot.md`, `instructions.md`, `gemini.md`) — preserved from `discovery_legacy.rs:185-186`.
2. **`*.instructions.md` suffix** — preserved from `discovery_legacy::classify_feature_kind`.
3. **`<engine>-instructions.md`** where engine ∈ {copilot, claude, agents, gemini} — closes the issue #725 silent-drop gap (`copilot-instructions.md`).

Implementation note (deviation from spec text): the spec suggested a `Lazy<Regex>` static. Since `regex` is not a direct dependency of libaipm and the pattern is fixed (one of four prefixes plus a literal suffix), I used `str::strip_suffix` + `matches!` over the prefix set instead. This avoids adding a new dep AND dodges the `.unwrap()` concern on `Lazy<Regex>` static init that the spec itself flagged. Updated feature-list.json step text accordingly.

Re-exported as `pub use instruction::classify as classify_instruction` from `discovery/mod.rs`.

Tests: 22 covering each Case A name (5), Case B suffix (3), Case C engine-prefix (4), case-insensitivity (3), negative cases (5: random.md, wrong-order, unknown-engine, wrong suffix, .bak extension, empty, just `-instructions.md`), and structural fields (path/engine pass-through, feature_dir = None for instructions).

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 89.87% (>= 89%)
- overall: PASS

## 2026-05-01 — Feature 6: Top-level classifier dispatch — DONE

Added `crates/libaipm/src/discovery/classify.rs` with `pub fn classify(path: &Path, project_root: &Path, _fs: &dyn Fs) -> Option<DiscoveredFeature>`.

Algorithm:
1. Extract file_name from path; None → return None.
2. Call `source::infer_engine_root(path, project_root)`; None → return None (no engine ancestor).
3. Try `instruction::classify(file_name, path, engine, source_root)` first — wins over the agent rule so that `agents/AGENTS.md` is classified as Instructions, not Agent (preserves today's classify_feature_kind precedence comment at discovery_legacy.rs:259-260).
4. Match on file_name:
   - `SKILL.md` → match_skill
   - `hooks.json` → match_hook
   - `aipm.toml` → match_plugin
   - `marketplace.json` → match_marketplace
   - `plugin.json` → match_plugin_json
   - `*.md` (case-insensitive via `path.extension().is_some_and(|ext| ext.eq_ignore_ascii_case("md"))`) → match_agent
   - else → None

The `_fs` parameter is reserved per spec for matchers that may need filesystem validation in later spec features. Today's matchers are path-only.

Re-exported as `pub use classify::classify as classify_path` from `discovery/mod.rs`.

Tests: 20 covering engine-root gating (no engine root → None, unrecognized filename → None), instruction precedence (uppercase AGENTS.md, copilot.md, copilot-instructions.md, *.instructions.md), filename dispatch matrix (SKILL.md, hooks.json variants, aipm.toml, marketplace.json, plugin.json, agent .md), engine attribution (Copilot, Ai), the issue #725 full tree (3 skills + copilot-instructions.md), and negative cases (SKILL.md without skills ancestor, random .md outside agents, aipm.toml outside .ai).

Cargo verifier (after fmt fix on a trailing comma):
- build/test/clippy/fmt: PASS
- coverage: 89.92% (>= 89%)
- overall: PASS

## 2026-05-01 — Feature 7: Walker — DONE

Added `crates/libaipm/src/discovery/walker.rs` with `pub fn walk(project_root, opts) -> Result<WalkResult, Error>`. Also added `pub struct DiscoverOptions { max_depth, source_filter, follow_symlinks }` (Debug, Default, Clone) to `discovery/mod.rs` ahead of feature 8.

Behavior:
- Wraps `ignore::WalkBuilder` with hidden(false) so .claude/.github/.ai are descended into; git_ignore/global/exclude all true; follow_links from opts; max_depth honored.
- filter_entry prunes directories whose name is in SKIP_DIRS (`node_modules`, `target`, `.git`, `vendor`, `__pycache__`, `dist`, `build`) and records a `SkipReason::SkipDirByName` via an Arc<Mutex<Vec<SkipReason>>> shared between the filter closure and the post-walk drain.
- Source filter is deferred to discover() (the spec's "fail closed" alternative); walker walks the full tree.
- Outputs sorted for deterministic ordering.

WalkResult: `{ files: Vec<PathBuf>, scanned_dirs: Vec<PathBuf>, skipped: Vec<SkipReason> }`.

Re-exported as `pub use walker::{walk, WalkResult}` from `discovery/mod.rs`.

Tests (11): empty dir, files+dirs, SKIP_DIRS pruning + recording, max_depth honored, dot-dirs visible, issue #725 tree visible end-to-end, gitignored files excluded (test creates an empty `.git/` dir so the ignore crate honors `.gitignore` per its docs), follow_symlinks=false on Unix, skip-reason path recording, deep tree without max_depth, sorted output.

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 89.99% (>= 89%)
- overall: PASS

## 2026-05-01 — Feature 8: Public discover() API + AIPM_UNIFIED_DISCOVERY flag — DONE

Added `pub fn discover(project_root, opts, fs) -> Result<DiscoveredSet, Error>` and `UNIFIED_DISCOVERY_ENV` const to `crates/libaipm/src/discovery/mod.rs`.

Behavior:
- Reads `AIPM_UNIFIED_DISCOVERY` env var; `"1"` → unified path; anything else → legacy path. Default off; the spec rollout flips this in feature 20 (Phase 2).
- `unified_discover`: calls `walker::walk` then `classify::classify` on each file, building DiscoveredSet. Emits `tracing::trace!` per classified feature and `tracing::debug!` per skipped path.
- `legacy_discover`: inline adapter (feature 9 will extract). Delegates to `discover_features` and maps each legacy DiscoveredFeature → new `types::DiscoveredFeature` (kind, engine inferred via `infer_engine_root`, layout=Canonical, source_root, feature_dir=parent, path). Features without an engine ancestor are dropped (small known divergence — same set the unified path would also drop).
- `apply_source_filter`: post-classification filter retaining features whose source_root file_name matches the option string.

Important correction discovered while writing tests: the research's "lint silently drops the #725 layout" was wrong about the SKILL files. Today's `discover_features` (lint's walker) already finds skills at `.github/copilot/skills/<x>/SKILL.md` via `classify_feature_kind`'s `grandparent_name == "skills"` branch. The actual lint-side silent drop in #725 is the `copilot-instructions.md` filename (case C in instruction.rs, fixed by feature 5). The migrate-side silent drop on the SKILL files is in `CopilotSkillDetector` (a different code path, not `discover_features`) and is fixed by feature 14's adapter switch.

Updated test naming/content accordingly:
- `discover_legacy_finds_issue_725_skill_files` (today's lint already finds them).
- `discover_legacy_drops_copilot_instructions_md_unified_finds_it` (the actual lint-side fix demonstration).

Tests (15 in discovery::tests): unified_enabled exact-"1" semantics, unified finds full #725 tree (3 skills + 1 instructions), legacy finds canonical skills, legacy finds #725 skill files, the legacy/unified instructions divergence, walker skipped dirs propagated, source_filter retains only matching, source_filter no-match → empty, empty root → empty set, apply_source_filter no-filter keeps all.

Tests serialize env-var manipulation via a `static ENV_LOCK: Mutex<()>` to avoid races with cargo's parallel test runner. PoisonError handling uses `unwrap_or_else(PoisonError::into_inner)` (not denied by clippy).

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 90.06% (>= 89%, crossed 90%)
- overall: PASS

## 2026-05-01 — Feature 9: legacy_compat module — DONE

Extracted the inline `legacy_discover` from feature 8 into a dedicated `crates/libaipm/src/discovery/legacy_compat.rs` module with `pub fn discover_features_compat(project_root, opts, fs) -> Result<DiscoveredSet, Error>`.

Behavior unchanged from feature 8's inline version:
- Delegates to today's `discover_features` (in `discovery_legacy.rs`).
- Maps each legacy DiscoveredFeature → new types::DiscoveredFeature by inferring engine via `source::infer_engine_root`, setting layout = Canonical (legacy doesn't know layouts), and using path.parent() as feature_dir.
- Drops features whose path has no recognized engine ancestor (small known divergence from today's behavior on project-root CLAUDE.md, etc. — same set the unified path also drops).
- Applies opts.source_filter post-classification.
- `_fs` parameter accepted for API symmetry (legacy uses its own filesystem access).

`discovery::discover()` now delegates the legacy branch to `legacy_compat::discover_features_compat`.

Tests added (8 in discovery::legacy_compat::tests):
- Empty root → empty set.
- Canonical skills (.claude/skills/x/SKILL.md AND .github/skills/y/SKILL.md) round-trip with correct engine/layout/source_root/feature_dir.
- #725 skill files visible via the legacy grandparent==skills branch.
- copilot-instructions.md dropped (the actual #725 lint-side bug; fix lives in the unified path).
- Project-root CLAUDE.md dropped (no engine ancestor — documented divergence).
- source_filter retains only matching engine.
- max_depth propagated to the legacy walker.
- .ai/ marketplace + plugin.json + aipm.toml all classified as Ai engine.

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 90.08% (>= 89%)
- overall: PASS

## 2026-05-01 — Feature 10: Lint integration — DONE

The customer-fix integration step. lint now calls the unified `discovery::discover` API instead of `discover_features` directly.

Key changes:
1. **`crate::discovery::DiscoveredFeature` re-export switched** from `discovery_legacy::DiscoveredFeature` (legacy shape) to `types::DiscoveredFeature` (new shape). Lint code now uses the new fields (`path`, `engine`, `layout`, `source_root`, `feature_dir`).
2. **`lint()` rewrite** at `crates/libaipm/src/lint/mod.rs`:
   - Calls `crate::discovery::discover(&opts.dir, &DiscoverOptions { max_depth, source_filter: opts.source.clone(), follow_symlinks: false }, fs)`.
   - Removes inline `--source` post-filter (now handled inside `discover()`).
   - Maps the new `source_root` (PathBuf) back to a `String` for `sources_scanned` via `file_name().to_string_lossy()`.
3. **`run_rules_for_feature` rewrite**: `feature.path` (was `file_path`); `is_inside_ai` now checks `engine == Engine::Ai` OR any `.ai` path component (preserves the legacy "any .ai ancestor" semantics for nested `.ai/<plugin>/.claude/` layouts where the new `infer_engine_root` returns the innermost engine).
4. **`Outcome` extended** with `scan_counts: ScanCounts` and `scanned_dirs: Vec<PathBuf>`. `#[derive(Default)]` added so callers can use `..Outcome::default()` spread.
5. **Fallback for root-level instruction files**: `classify::classify` AND `legacy_compat::discover_features_compat` now use `(Engine::Ai, project_root)` when `infer_engine_root` returns None, so files like `/repo/CLAUDE.md` still classify as Instructions in both unified and legacy paths. Preserves today's behavior on the two existing tests `lint_discovers_oversized_instruction_file` and `lint_config_overrides_thresholds`.

Test infrastructure:
- New `discovery/test_env.rs` (cfg(test)) module with `pub(crate) fn with_unified_discovery_env` and a shared static Mutex<()>. Both `discovery::tests` and `lint::tests` use it to serialize `AIPM_UNIFIED_DISCOVERY` env-var manipulation across modules.

Tests:
- 33 reporter.rs `Outcome { ... }` constructions updated via `..Outcome::default()` spread (delegated to general-purpose sub-agent).
- 1 lint::tests::lint_outcome_default_counts updated to spread + assert new fields.
- legacy_compat::tests::project_root_instructions_dropped → renamed to project_root_instructions_kept_with_ai_fallback, with new assertions on the (Engine::Ai, project_root) fallback.
- 4 new lint::tests for issue #725:
  - `lint_unified_finds_issue_725_skills_and_instructions` — unified mode: 3 skills + 1 instructions.
  - `lint_legacy_finds_issue_725_skills_but_drops_copilot_instructions` — legacy mode: 3 skills + 0 instructions (the silent drop the unified path fixes).
  - `lint_outcome_carries_scan_counts_and_dirs` — Outcome carries the new fields.
  - `lint_source_filter_propagated_to_discover` — source_filter handled inside discover().

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 90.08% (>= 89%)
- overall: PASS

## 2026-05-01 — Feature 11: Migrate Adapter trait + adapters::all() registry — DONE

Added `crates/libaipm/src/migrate/adapters/mod.rs` with:
- `pub trait Adapter: Send + Sync` with three methods: `name()`, `applies_to(&DiscoveredFeature) -> bool`, `to_artifact(&DiscoveredFeature, &dyn Fs) -> Result<Artifact, Error>`.
- `pub fn all() -> Vec<Box<dyn Adapter>>` returning an empty vec for now. The 12 adapters land in features 12 (Copilot ×6) and 13 (Claude ×6).
- Module-level doc-comment specifies first-match-wins resolution policy: orchestrator iterates the registry in order and selects the first adapter whose applies_to returns true.

The legacy Detector trait at `migrate/detector.rs` stays ALIVE during this transition; feature 14's orchestrator switch is what migrates the actual call sites.

Wired `pub mod adapters` into `migrate/mod.rs`.

Tests (4):
- `registry_is_a_vec_of_box_dyn_adapter` — type sanity check via inferred return type.
- `registry_iteration_is_safe_when_empty` — empty vec iteration completes without panic.
- `stub_adapter_does_not_apply` — a test-only StubAdapter has stable name and applies_to returns false.
- `adapter_trait_is_object_safe` — Box<dyn Adapter> compiles and dispatches via dyn (guards against accidental generic methods that would break object safety).

Cargo verifier:
- build/test/clippy/fmt: PASS
- coverage: 90.08% (>= 89%)
- overall: PASS
