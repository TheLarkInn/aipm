## 2026-04-03 — Feature 1: Extend Diagnostic struct with col, end_line, end_col fields

Status: COMPLETE

Changes:
- Added `col: Option<usize>`, `end_line: Option<usize>`, `end_col: Option<usize>` to Diagnostic struct in diagnostic.rs
- Updated 17 Diagnostic struct literal constructions across 14 files:
  - diagnostic.rs (1 test literal)
  - reporter.rs (2 test literals)
  - 12 rule files (1-2 production literals each)
- All new fields set to `None` — purely additive, no behavioral change

Verification:
- cargo build --workspace: OK
- cargo test --workspace: 74 scenarios, 354 steps all pass
- cargo clippy --workspace -- -D warnings: clean
- cargo fmt --check: clean

## 2026-04-03 — Feature 2: Extend Rule trait with help_url() and help_text() default methods

Status: COMPLETE

Changes:
- Added `help_url(&self) -> Option<&'static str>` with default `None` to Rule trait in rule.rs
- Added `help_text(&self) -> Option<&'static str>` with default `None` to Rule trait in rule.rs
- Default method impls mean zero breakage for all 12 existing rule implementations

Verification:
- cargo build --workspace: OK
- cargo test --workspace: 1103 unit + 74 BDD scenarios all pass
- cargo clippy --workspace -- -D warnings: clean
