---
date: 2026-05-05
researcher: Sean Larkin
git_commit: ad39977
branch: main
repository: aipm
topic: "engine catalog and folder mapping"
tags:
  - research
  - codebase
  - engines
  - libaipm-engine-spec
status: complete
---

# Engine Catalog and Folder Mapping (`libaipm-engine-spec`)

This document catalogs every engine `aipm` knows about today, the canonical
Rust types that describe them, the schema-data file that drives code
generation, the `build.rs` emitted const tables, the engine→filesystem-root
mapping, and every consumer of `libaipm_engine_spec` in the workspace.

All references are to commit `ad39977` on branch `main`.

---

## 1. Canonical Rust types in `crates/libaipm-engine-spec/src/types.rs`

File: `/workspaces/aipm/crates/libaipm-engine-spec/src/types.rs`

### Top-level constants and root document

- `pub const META_SCHEMA_VERSION: &str = "1.0.0";` — `src/types.rs:15`
- `pub struct EngineApiSchemaFile` — `src/types.rs:18-32`. Fields:
  - `schema_uri: String` (rename `$schema`) — line 23
  - `meta_schema_version: String` — line 24
  - `generated_at: String` — line 26
  - `engines: Vec<EngineBootstrap>` — line 27
  - `versions: BTreeMap<String, String>` — line 28
  - `apis: BTreeMap<String, EngineApi>` — line 29
  - `tool_compatibility: ToolCompatibility` — line 30
  - `suggestions: BTreeMap<String, EngineSuggestions>` — line 31

### Per-engine bootstrap and API

- `pub struct EngineBootstrap` — `src/types.rs:35-44`:
  - `name: String` (e.g. `"claude"`, `"copilot-cli"`) — line 39
  - `source: String` (e.g. `"npm"`) — line 41
  - `package: String` (e.g. `"@anthropic-ai/claude-code"`) — line 43

- `pub struct EngineApi` — `src/types.rs:47-81`. Fields:
  - `manifest_fields: Vec<ManifestFieldSpec>` — line 50
  - `manifest_search_paths: Vec<String>` (default empty) — line 52
  - `settings_paths: Vec<String>` — line 53
  - `folder_conventions: Vec<String>` — line 54
  - `convention_files: Vec<ConventionFile>` — line 55
  - `skill_registration: serde_json::Value` (default null) — line 57
  - `lsp_config: serde_json::Value` — line 59
  - `mcp_config: serde_json::Value` — line 61
  - `output_styles: Vec<String>` — line 63
  - `size_limits: SizeLimits` — line 64
  - `detection_notes`/`discovery_notes`/`rule_notes: Vec<String>` — lines 67/70/73 (documentation only)
  - `tool_calls: Vec<ToolCall>` — line 74
  - `hook_events: Vec<HookEvent>` — line 75
  - `agent_commands: Vec<String>` — line 77
  - `feature_flags: Vec<String>` — line 79
  - `features: Vec<FeatureSpec>` — line 80

### Supporting structures

- `pub struct ManifestFieldSpec` — `src/types.rs:86-96` (`name`, `type`, `required`, `constraints`, `notes`)
- `pub struct Constraints` — `src/types.rs:99-110` (`max_length`, `min_length`, `regex`, `default`)
- `pub struct ConventionFile` — `src/types.rs:113-118` (`filename`, `convention_paths: Vec<String>`)
- `pub struct SizeLimits` — `src/types.rs:125-131` (flattened `numeric: BTreeMap<String, u64>` + optional `notes`)
- `pub struct ToolCall` — `src/types.rs:134-144` (`name`, `aliases`, `deprecated`, `notes`)
- `pub struct HookEvent` — `src/types.rs:147-157` (`name`, `aliases`, `deprecated`, `notes`)
- `pub struct FeatureSpec` — `src/types.rs:160-170` (`kind: FeatureKind`, `manifest_field`, `layout_hint`, `capabilities`)
- `pub enum FeatureKind` — `src/types.rs:174-185` with variants:
  `Skill`, `Agent`, `Mcp`, `Hook`, `OutputStyle`, `Lsp`, `Extension`, `Command` (snake_case-serialized)
- `bitflags! pub struct EngineFeatureSet: u8` — `src/types.rs:187-202`. Bits:
  `SKILL = 0b0000_0001`, `AGENT = 0b0000_0010`, `MCP = 0b0000_0100`,
  `HOOK = 0b0000_1000`, `OUTPUT_STYLE = 0b0001_0000`, `LSP = 0b0010_0000`,
  `EXTENSION = 0b0100_0000`, `COMMAND = 0b1000_0000`
- `pub struct ToolCompatibility` — `src/types.rs:206-210` (`shared_tools: Vec<String>`, `engine_exclusive_tools: BTreeMap<String, ToolSupport>`)
- `pub struct ToolSupport` — `src/types.rs:213-219` (`supported_by`/`unsupported_by: Vec<String>`)
- `pub struct EngineSuggestions` — `src/types.rs:222-231`
- `pub struct HookEventStatic` — `src/types.rs:236-242` (`&'static str`-only fields, used in generated tables)
- `pub struct EngineSpec` — `src/types.rs:252-263`. **This is the static const-table form of an engine** with these `&'static`-typed fields:
  - `name: &'static str`
  - `package: &'static str`
  - `version: &'static str`
  - `marker_paths: &'static [&'static str]`
  - `marketplace_manifest_path: &'static str`
  - `manifest_search_paths: &'static [&'static str]`
  - `settings_paths: &'static [&'static str]`
  - `folder_conventions: &'static [&'static str]`
  - `convention_files: &'static [(&'static str, &'static [&'static str])]`

> Note: There is **no hand-written `Engine` enum or list of variants in
> `types.rs`**. The `Engine` enum is generated by `build.rs` from the data
> file's `engines` array and `include!`d at `src/generated.rs:9`.

---

## 2. `crates/libaipm-engine-spec/src/lib.rs` — public re-exports

File: `/workspaces/aipm/crates/libaipm-engine-spec/src/lib.rs`

Re-exports from `generated`:
`Engine`, `EngineSet`, `ENGINES`, `FEATURES_BY_ENGINE`,
`HOOK_EVENTS_BY_ENGINE`, `TOOL_COMPATIBILITY`, `VALID_TOOLS`, `constraints`,
`paths` — `src/lib.rs:15-19`

Re-exports from `helpers`:
`engine_for_root_dir`, `is_valid_event`, `marketplace_host_for_root_dir`,
`suggest_canonical`, `valid_tool_name_check`, `MarketplaceHost`,
`ToolNameViolation` — `src/lib.rs:20-23`

Re-exports from `types`:
`EngineApiSchemaFile`, `EngineFeatureSet`, `EngineSpec`, `FeatureKind`,
`HookEventStatic`, `META_SCHEMA_VERSION` — `src/lib.rs:24-27`

Smoke tests in `src/lib.rs:29-235` document the shape of the generated
tables (see e.g. lines 38-42, 70-81, 202-225).

---

## 3. `crates/libaipm-engine-spec/data/engine-api-schema.json` — engine catalog

File: `/workspaces/aipm/crates/libaipm-engine-spec/data/engine-api-schema.json`

Document header — lines 1-4:
```
"$schema": "../../../schemas/engine-api.schema.json"
"meta_schema_version": "1.0.0"
"generated_at": "2026-05-05T13:04:12Z"
```

### Engines array (lines 5-16) — exactly two engines

| Field    | claude (lines 6-10)             | copilot-cli (lines 11-15) |
|----------|---------------------------------|----------------------------|
| `name`   | `claude`                        | `copilot-cli`              |
| `source` | `npm`                           | `npm`                      |
| `package`| `@anthropic-ai/claude-code`     | `@github/copilot`          |

### Versions (lines 17-20)

| Engine        | Version    |
|---------------|-----------|
| `claude`      | `2.1.128` |
| `copilot-cli` | `1.0.40`  |

### `apis.claude` (lines 22-370)

- `manifest_fields: []` — line 23 (empty)
- `settings_paths` (lines 24-27):
  - `.claude/settings.json`
  - `.claude/settings.local.json`
- `folder_conventions` (lines 28-32):
  - `.claude/`
  - `.claude/skills/`
  - `~/.claude/skills/`
- `convention_files` (lines 33-41): one entry — `CLAUDE.md` with `convention_paths` `[".", ".claude"]`
- `skill_registration.notes` — line 43
- `lsp_config: {}` — line 45
- `mcp_config.notes` — line 47
- `output_styles: []` — line 49
- `size_limits.bash_timeout_max_ms: 600000` — line 51
- `detection_notes` — `Presence of CLAUDE.md in repo root or .claude/ folder` (line 55)
- `tool_calls` (lines 61-211): `Task`, `Bash`, `Edit`, `Read`, `Write`, `Glob`, `Grep`, `WebFetch`, `WebSearch`, `TodoWrite`, `mcp`, `list_mcp_resources`, `read_mcp_resource`, `notebook_edit`, `ask_user_question`, `enter_worktree`, `exit_worktree`, `exit_plan_mode`, `task_output`, `task_stop`
- `hook_events` (lines 211-347, 27 events): `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `Notification`, `SessionStart`, `Stop`, `StopFailure`, `SubagentStart`, `SubagentStop`, `PreCompact`, `PostCompact`, `SessionEnd`, `PermissionRequest`, `Setup`, `TeammateIdle`, `TaskCreated`, `TaskCompleted`, `UserPromptSubmit`, `ToolError`, `Elicitation`, `ElicitationResult`, `ConfigChange`, `InstructionsLoaded`, `WorktreeCreate`, `WorktreeRemove`, `CwdChanged`, `FileChanged`
- `features` (lines 348-369): `skill`, `agent`, `mcp`, `hook`, `output_style`

### `apis.copilot-cli` (lines 371-1376)

- `manifest_fields` (lines 372-478): `name` (max 64, regex `^[a-zA-Z0-9-]+$`), `source`, `description` (max 1024), `version`, `author`, `homepage` (url), `repository` (url), `license`, `keywords`, `category`, `tags`, `commands`, `agents`, `skills`, `hooks`, `mcpServers`, `lspServers`, `outputStyles`, `logo`, `postInstallMessage` (max 2048), `strict` (default true)
- `manifest_search_paths` (lines 479-484):
  - `marketplace.json`
  - `.plugin/marketplace.json`
  - `.github/plugin/marketplace.json`
  - `.claude-plugin/marketplace.json`
- `settings_paths` (lines 485-491):
  - `.github/copilot/settings.json`
  - `.github/copilot/settings.local.json`
  - `.claude/settings.json`
  - `.claude/settings.local.json`
  - `~/.copilot/mcp-config.json`
- `folder_conventions` (lines 492-512):
  - `.github/copilot/`
  - `.github/extensions/`
  - `.github/skills/`
  - `.github/agents/`
  - `.github/plugin/`
  - `.github/lsp.json`
  - `.github/mcp.json`
  - `.github/copilot-instructions.md`
  - `.github/instructions/**/*.instructions.md`
  - `.claude/`
  - `.claude/skills/`
  - `.claude-plugin/`
  - `.agents/`
  - `.agents/skills/`
  - `~/.copilot/`
  - `~/.copilot/extensions/`
  - `~/.copilot/skills/`
  - `~/.copilot/logs/`
  - `~/.claude/skills/`
- `convention_files` (lines 513-539): four entries — `copilot-instructions.md` in `.github`, `AGENTS.md` in `.`, `CLAUDE.md` in `.`/`.claude`, `GEMINI.md` in `.`
- `tool_calls` (lines 638+): includes `bash`, `glob`, `grep`, `web_fetch`, `git_apply_patch`, GitHub API tools (`get_pull_request`, `list_pull_requests`, etc.), `browser_navigate` and other browser tools
- `hook_events` (lines 1280-1350, 10 events): `sessionStart`/aliased `SessionStart`; `sessionEnd`/`SessionEnd`; `userPromptSubmitted`/`UserPromptSubmit`; `preToolUse`/`PreToolUse`; `postToolUse`/`PostToolUse`; `errorOccurred`/`PostToolUseFailure`/`ErrorOccurred`; `agentStop`/`Stop`; `subagentStop`/`SubagentStop`; `subagentStart`; `preCompact`/`PreCompact`
- `features` (lines 1351-1375): `skill`, `agent`, `mcp`, `hook`, `lsp`, `extension`

### Tool compatibility, suggestions

- `tool_compatibility` begins at line 1378 with `shared_tools` then `engine_exclusive_tools`.
- `suggestions.claude.adaptor_fixes`/`test_cases`/`behaviour_variants` — lines 2080-2098.
- `suggestions.copilot-cli.*` — lines 2099-2131.

**Engines present in this data file: exactly `claude` and `copilot-cli`. There are no entries for cursor, gemini, opencode, or any other engine.**

---

## 4. `crates/libaipm-engine-spec/build.rs` — what is emitted to `OUT_DIR/engine_data.rs`

File: `/workspaces/aipm/crates/libaipm-engine-spec/build.rs`

Top-level pipeline (`build.rs:50-65`):
1. `emit_rerun_directives()` — `build.rs:67-79` rerun on data file, meta-schema, `src/types.rs`, `build.rs`.
2. `load_and_validate()` — `build.rs:81-110` reads `data/engine-api-schema.json`, validates against `../../schemas/engine-api.schema.json` via `jsonschema::validator_for`, deserializes into `EngineApiSchemaFile`, asserts `parsed.meta_schema_version == META_SCHEMA_VERSION`.
3. `generate_engine_module(&parsed)` — `build.rs:112-136` produces a single `TokenStream` containing all const tables.
4. Output is `prettyplease::unparse`d and written to `OUT_DIR/engine_data.rs`.
5. A sibling `OUT_DIR/valid_tools.rs` is written by `write_valid_tools_phf` — `build.rs:539-563`.

### Generated items (in order, `build.rs:122-135`)

#### `pub enum Engine` + `impl Engine` — `build.rs:138-187`
- One variant per `parsed.engines[].name`, mapped through `to_pascal_case` (`build.rs:565-575`). Concretely: `Claude`, `CopilotCli`.
- `pub const ALL: &'static [Self] = &[ Self::Claude, Self::CopilotCli ]` — line 158.
- `pub const fn name(self) -> &'static str` — match each variant to its lowercase kebab-case name (`"claude"`, `"copilot-cli"`) — lines 161-165.
- `pub fn from_name(name: &str) -> Option<Self>` — reverse lookup — lines 167-173.
- `pub const fn as_set(self) -> EngineSet` — variant→bit, e.g. `Engine::Claude => EngineSet::CLAUDE` — lines 179-184.

#### `bitflags! pub struct EngineSet: u32` — `build.rs:189-211`
- One flag per engine, named with `to_screaming_snake` (`build.rs:577-579`): `CLAUDE = 1`, `COPILOT_CLI = 2`.
- `const ALL = Self::CLAUDE.bits() | Self::COPILOT_CLI.bits()` — line 207.

#### `pub const ENGINES: &[(Engine, EngineSpec)]` — `build.rs:213-220, 222-267`
Each entry constructed by `generate_engine_entry`:
- `name` — `bootstrap.name` (e.g. `"claude"`)
- `package` — `bootstrap.package`
- `version` — looked up in `parsed.versions[name]`
- `marker_paths` — hand-rolled in `marker_paths_for(engine_name)` at `build.rs:509-517`:
  - `"claude"` → `[".claude-plugin/plugin.json", ".claude-plugin/plugin.toml"]`
  - `"copilot-cli"` → `["plugin.json", ".github/plugin/plugin.json", ".claude-plugin/plugin.json"]`
  - `_` → `&[]`
- `marketplace_manifest_path` — hand-rolled in `marketplace_manifest_path_for` at `build.rs:524-530`:
  - `"claude"` → `".claude-plugin/marketplace.toml"`
  - `"copilot-cli"` → `".github/plugin/marketplace.json"`
  - `_` → `""`
- `manifest_search_paths`, `settings_paths`, `folder_conventions`, `convention_files` — pulled directly from the matching `parsed.apis[name]`.

#### `pub const TOOL_COMPATIBILITY: &[(&str, EngineSet)]` — `build.rs:273-301`
- One row per name in `tool_compatibility.shared_tools`, paired with `EngineSet::ALL`.
- Followed by one row per `tool_compatibility.engine_exclusive_tools[name]`, paired with the `EngineSet` union of that tool's `supported_by` list (built by `engine_set_expression`, `build.rs:492-502`).

#### `pub const HOOK_EVENTS_BY_ENGINE: &[(Engine, &[HookEventStatic])]` — `build.rs:303-324`
- One tuple per engine; the inner slice is built from `api.hook_events` via `hook_event_token` (`build.rs:326-340`) which copies `name`, `aliases`, `deprecated`, optional `notes` into static-string form.

#### `pub const FEATURES_BY_ENGINE: &[(Engine, EngineFeatureSet)]` — `build.rs:439-460`
- One tuple per engine; the `EngineFeatureSet` is the union (`feature_set_expression`, `build.rs:462-473`) of bit constants chosen from `api.features[].kind` via `feature_kind_bit_name` (`build.rs:475-486`):
  - `Skill→SKILL`, `Agent→AGENT`, `Mcp→MCP`, `Hook→HOOK`, `OutputStyle→OUTPUT_STYLE`, `Lsp→LSP`, `Extension→EXTENSION`, `Command→COMMAND`.

#### `pub mod paths { … }` — `build.rs:349-368`
**Hard-coded module body**, not derived from the data file. Constants emitted:
- `CLAUDE_PLUGIN_DIR = ".claude-plugin"` — line 354
- `GITHUB_PLUGIN_DIR = ".github/plugin"` — line 355
- `MARKETPLACE_JSON = "marketplace.json"` — line 356
- `MARKETPLACE_TOML = "marketplace.toml"` — line 357
- `PLUGIN_JSON = "plugin.json"` — line 358
- `PLUGIN_TOML = "plugin.toml"` — line 359
- `AIPM_TOML = "aipm.toml"` — line 360
- `SETTINGS_JSON = "settings.json"` — line 361
- `SETTINGS_LOCAL_JSON = "settings.local.json"` — line 362
- `CLAUDE_DOT = ".claude"` — line 363
- `GITHUB_DOT = ".github"` — line 364
- `AI_DOT = ".ai"` — line 365

Verified by smoke tests at `src/lib.rs:202-216`.

#### `pub mod constraints { … }` — `build.rs:377-418`
Aggregated from `api.manifest_fields[].constraints` whose `name` matches a known prefix in `constraint_const_name` (`build.rs:430-437`):
- `"name"` → `PLUGIN_NAME`
- `"description"` → `DESCRIPTION`
- `"postInstallMessage"` → `POST_INSTALL_MSG`

For each matched field with at least one of `max_length`/`min_length`/`regex`, emits constants `<PREFIX>_MAX_LEN: usize`, `<PREFIX>_MIN_LEN: usize`, `<PREFIX>_REGEX: &str`. Concrete values (smoke test in `src/lib.rs:218-225`):
- `PLUGIN_NAME_MAX_LEN = 64`
- `PLUGIN_NAME_REGEX = "^[a-zA-Z0-9-]+$"`
- `DESCRIPTION_MAX_LEN = 1024`
- `POST_INSTALL_MSG_MAX_LEN = 2048`

#### `pub static VALID_TOOLS: phf::Set<&'static str>` — `build.rs:539-563`
- Written to a sibling file `OUT_DIR/valid_tools.rs`.
- Built by sort-deduping every `tc.name` and every alias in `tc.aliases` across every `api.tool_calls` entry.
- `include!`d via `mod valid_tools_inner` in `src/generated.rs:11-18`.

---

## 5. Engine→filesystem-root mapping (every piece of evidence)

### A. Hard-coded in `helpers::engine_for_root_dir`

File: `/workspaces/aipm/crates/libaipm-engine-spec/src/helpers.rs`

```rust
pub fn engine_for_root_dir(name: &str) -> Option<Engine> {
    if name == paths::CLAUDE_DOT {
        Some(Engine::Claude)
    } else if name == paths::GITHUB_DOT {
        Some(Engine::CopilotCli)
    } else {
        None
    }
}
```
— `src/helpers.rs:41-49`.

`paths::CLAUDE_DOT == ".claude"` and `paths::GITHUB_DOT == ".github"` (build.rs:363-364), so:

| Engine variant       | Root directory recognised | Source                     |
|----------------------|---------------------------|----------------------------|
| `Engine::Claude`     | `.claude`                 | `src/helpers.rs:42-43`     |
| `Engine::CopilotCli` | `.github`                 | `src/helpers.rs:44-45`     |
| `MarketplaceHost::Ai`| `.ai`                     | `src/helpers.rs:55-61`     |

`marketplace_host_for_root_dir` is the sibling helper for `.ai`, which is intentionally *not* an engine — `src/helpers.rs:11-20, 55-61`.

Tests confirming this map: `src/helpers.rs:111-133`.

### B. Schema-driven `folder_conventions` (per engine)

For Claude — `data/engine-api-schema.json:28-32` —
`.claude/`, `.claude/skills/`, `~/.claude/skills/`.

For Copilot CLI — `data/engine-api-schema.json:492-512` — 19 entries including
`.github/copilot/`, `.github/skills/`, `.github/agents/`, `.github/plugin/`,
`.claude/`, `.claude-plugin/`, `.agents/`, `~/.copilot/`, `~/.claude/skills/`, etc.

These flow into `EngineSpec.folder_conventions` via `build.rs:234, 262`.

### C. Schema-driven `settings_paths`

- Claude: `.claude/settings.json`, `.claude/settings.local.json` — `data/engine-api-schema.json:24-27`.
- Copilot: `.github/copilot/settings.json`, `.github/copilot/settings.local.json`, `.claude/settings.json`, `.claude/settings.local.json`, `~/.copilot/mcp-config.json` — `data/engine-api-schema.json:485-491`.

### D. Hard-coded in `build.rs::marker_paths_for`

`build.rs:509-517`:
- `claude` → `.claude-plugin/plugin.json`, `.claude-plugin/plugin.toml`
- `copilot-cli` → `plugin.json`, `.github/plugin/plugin.json`, `.claude-plugin/plugin.json`

### E. Hard-coded in `build.rs::marketplace_manifest_path_for`

`build.rs:524-530`:
- `claude` → `.claude-plugin/marketplace.toml`
- `copilot-cli` → `.github/plugin/marketplace.json`

Asserted by smoke tests at `src/lib.rs:70-81` and by `crates/libaipm/src/engine.rs:312-318`.

### F. Display name (legacy capitalization)

`crates/libaipm/src/engine.rs:42-47`:
- `Engine::Claude` → `"Claude"`
- `Engine::CopilotCli` → `"Copilot"`

### Summary table

| Engine variant       | Schema name   | Package                       | Root dir(s) recognised by `engine_for_root_dir` | Marker paths                                                                                  | Marketplace manifest                       | Display name |
|----------------------|---------------|-------------------------------|--------------------------------------------------|-----------------------------------------------------------------------------------------------|--------------------------------------------|--------------|
| `Engine::Claude`     | `claude`      | `@anthropic-ai/claude-code`   | `.claude`                                        | `.claude-plugin/plugin.json`, `.claude-plugin/plugin.toml`                                   | `.claude-plugin/marketplace.toml`          | `Claude`     |
| `Engine::CopilotCli` | `copilot-cli` | `@github/copilot`             | `.github`                                        | `plugin.json`, `.github/plugin/plugin.json`, `.claude-plugin/plugin.json`                    | `.github/plugin/marketplace.json`          | `Copilot`    |

The `.ai` directory is recognised separately as `MarketplaceHost::Ai`, *not* an engine — `src/helpers.rs:16-20`.

No other engines (cursor, gemini, opencode, etc.) are present in any of: `data/engine-api-schema.json`, `engine_for_root_dir`, `marker_paths_for`, or `marketplace_manifest_path_for`.

---

## 6. Consumers of `libaipm_engine_spec`

### Cargo dependency wiring

- Workspace declaration: `Cargo.toml:26` — `libaipm-engine-spec = { path = "crates/libaipm-engine-spec", version = "0.24.0" }`.
- `crates/libaipm/Cargo.toml:13` — `libaipm-engine-spec = { workspace = true }`.
- `crates/aipm/Cargo.toml` — **does not directly depend** on `libaipm-engine-spec`. It depends on `libaipm` only and reaches engine-spec items through the re-exports at `crates/libaipm/src/lib.rs:44`.

### Consumers in `crates/libaipm/`

Re-exports:
- `crates/libaipm/src/lib.rs:44` — `pub use libaipm_engine_spec::{constraints, paths, Engine, EngineSet, MarketplaceHost};`

Engine table lookups (`ENGINES`, `marker_paths`, `marketplace_manifest_path`):
- `crates/libaipm/src/engine.rs:12, 16, 21, 29, 33`
- `crates/libaipm/src/discovery/instruction.rs:27, 33`

Helpers (`engine_for_root_dir`, `marketplace_host_for_root_dir`):
- `crates/libaipm/src/discovery/source.rs:13`

Type re-exports (`Engine`, `MarketplaceHost`):
- `crates/libaipm/src/discovery/types.rs:9`

`paths::*` constants:
- `crates/libaipm/src/discovery/layout.rs:28`
- `crates/libaipm/src/discovery_legacy.rs:52` (`paths::CLAUDE_DOT`)
- `crates/libaipm/src/lint/mod.rs:32-38` (uses `CLAUDE_DOT`, `GITHUB_DOT`, `AI_DOT` for source-root scanning)
- `crates/libaipm/src/lint/rules/scan.rs:8`
- `crates/libaipm/src/migrate/mod.rs:9`
- `crates/libaipm/src/migrate/detector.rs:5`
- `crates/libaipm/src/migrate/copilot_lsp_detector.rs:7`
- `crates/libaipm/src/migrate/hook_detector.rs:5`

Constraint constants:
- `crates/libaipm/src/manifest/validate.rs:404` — `constraints::PLUGIN_NAME_REGEX`
- `crates/libaipm/src/lint/rules/skill_name_too_long.rs:7` — `constraints::PLUGIN_NAME_MAX_LEN`
- `crates/libaipm/src/lint/rules/skill_desc_too_long.rs:7` — `constraints::DESCRIPTION_MAX_LEN`
- `crates/libaipm/src/lint/rules/skill_name_invalid.rs:118` — `constraints::PLUGIN_NAME_REGEX`

Hook-event helpers (`is_valid_event`, `suggest_canonical`):
- `crates/libaipm/src/lint/rules/hook_legacy_event.rs:7`
- `crates/libaipm/src/lint/rules/hook_unknown_event.rs:8`

Tool-name validation (`valid_tool_name_check`, `ToolNameViolation`, `EngineSet`):
- `crates/libaipm/src/lint/rules/valid_tool_name.rs:5-6, 18`

`EngineSet` use in manifest types and tests:
- `crates/libaipm/src/manifest/types.rs:6, 71, 324`
- `crates/libaipm/src/manifest/mod.rs:575, 612, 628`

Feature-set tables (`FEATURES_BY_ENGINE`, `EngineFeatureSet`, `FeatureKind`):
- `crates/libaipm/src/make/engine_features.rs:5, 9, 13, 18, 118, 148`

### Consumers in `crates/aipm/`

A `grep` for `libaipm_engine_spec` or `libaipm-engine-spec` across `crates/aipm/src/` and `crates/aipm/Cargo.toml` returns **no matches**. The CLI binary uses the `libaipm` re-exports (e.g. `libaipm::Engine`, `libaipm::workspace_init::adaptors::defaults()`) rather than importing `libaipm_engine_spec` directly. Confirmed by:
- `grep -rn "libaipm_engine_spec\|libaipm-engine-spec" /workspaces/aipm/crates/aipm/` — empty.

### `aipm init` folder-creation context (for cross-reference)

- `crates/aipm/src/main.rs:413` — `let adaptors = libaipm::workspace_init::adaptors::defaults();`
- `crates/libaipm/src/workspace_init/adaptors/mod.rs:13-15` — `pub fn defaults() -> Vec<Box<dyn ToolAdaptor>> { vec![Box::new(claude::Adaptor)] }`. The default set contains exactly one adaptor (Claude).
- `crates/libaipm/src/workspace_init/adaptors/claude.rs:26-29` — the Claude adaptor calls `fs.create_dir_all(&dir.join(".claude"))` unconditionally.
- `crates/libaipm/src/workspace_init/mod.rs:108-117` — `init()` iterates every adaptor returned by `defaults()` and applies it whenever `opts.marketplace` is true. There is no per-engine gating between the adaptor list and the workspace-init flow.

---

## File index (absolute paths)

- `/workspaces/aipm/crates/libaipm-engine-spec/src/types.rs`
- `/workspaces/aipm/crates/libaipm-engine-spec/src/lib.rs`
- `/workspaces/aipm/crates/libaipm-engine-spec/src/generated.rs`
- `/workspaces/aipm/crates/libaipm-engine-spec/src/helpers.rs`
- `/workspaces/aipm/crates/libaipm-engine-spec/build.rs`
- `/workspaces/aipm/crates/libaipm-engine-spec/data/engine-api-schema.json`
- `/workspaces/aipm/crates/libaipm-engine-spec/Cargo.toml`
- `/workspaces/aipm/Cargo.toml`
- `/workspaces/aipm/crates/libaipm/Cargo.toml`
- `/workspaces/aipm/crates/libaipm/src/lib.rs`
- `/workspaces/aipm/crates/libaipm/src/engine.rs`
- `/workspaces/aipm/crates/libaipm/src/discovery/layout.rs`
- `/workspaces/aipm/crates/libaipm/src/discovery/instruction.rs`
- `/workspaces/aipm/crates/libaipm/src/discovery/source.rs`
- `/workspaces/aipm/crates/libaipm/src/discovery/types.rs`
- `/workspaces/aipm/crates/libaipm/src/discovery_legacy.rs`
- `/workspaces/aipm/crates/libaipm/src/lint/mod.rs`
- `/workspaces/aipm/crates/libaipm/src/lint/rules/scan.rs`
- `/workspaces/aipm/crates/libaipm/src/lint/rules/hook_legacy_event.rs`
- `/workspaces/aipm/crates/libaipm/src/lint/rules/hook_unknown_event.rs`
- `/workspaces/aipm/crates/libaipm/src/lint/rules/valid_tool_name.rs`
- `/workspaces/aipm/crates/libaipm/src/lint/rules/skill_name_too_long.rs`
- `/workspaces/aipm/crates/libaipm/src/lint/rules/skill_desc_too_long.rs`
- `/workspaces/aipm/crates/libaipm/src/lint/rules/skill_name_invalid.rs`
- `/workspaces/aipm/crates/libaipm/src/manifest/mod.rs`
- `/workspaces/aipm/crates/libaipm/src/manifest/types.rs`
- `/workspaces/aipm/crates/libaipm/src/manifest/validate.rs`
- `/workspaces/aipm/crates/libaipm/src/migrate/mod.rs`
- `/workspaces/aipm/crates/libaipm/src/migrate/detector.rs`
- `/workspaces/aipm/crates/libaipm/src/migrate/hook_detector.rs`
- `/workspaces/aipm/crates/libaipm/src/migrate/copilot_lsp_detector.rs`
- `/workspaces/aipm/crates/libaipm/src/make/engine_features.rs`
- `/workspaces/aipm/crates/libaipm/src/workspace_init/mod.rs`
- `/workspaces/aipm/crates/libaipm/src/workspace_init/adaptors/mod.rs`
- `/workspaces/aipm/crates/libaipm/src/workspace_init/adaptors/claude.rs`
- `/workspaces/aipm/crates/aipm/src/main.rs`
